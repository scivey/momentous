// Generated by CoffeeScript 1.6.3
(function() {
  var addersubber, enders, flatSplat, generators, helpers, inDir, inLib, misc, moment, momentous, oneRand, partial2, path, predicates, printem, randDates, reverse2, selectors, sorters, starters, toStrings, _, _ref,
    __slice = [].slice;

  moment = require("moment");

  _ = require("underscore");

  path = require("path");

  _ref = require(path.join(__dirname, "lib/util.js")), reverse2 = _ref.reverse2, partial2 = _ref.partial2, flatSplat = _ref.flatSplat, inDir = _ref.inDir;

  inLib = inDir(path.join(__dirname, "lib"));

  momentous = {};

  predicates = require(inLib("predicates.js"));

  _.extend(momentous, predicates);

  selectors = require(inLib("selectors.js"));

  _.extend(momentous, selectors);

  addersubber = require(inLib("addersubber.js"));

  _.extend(momentous, addersubber);

  starters = require(inLib("starters.js"));

  _.extend(momentous, starters);

  enders = require(inLib("enders.js"));

  _.extend(momentous, enders);

  sorters = require(inLib("sorters.js"));

  momentous.sort = sorters.ascending;

  _.extend(momentous.sort, sorters);

  helpers = require(inLib("helpers.js"));

  _.extend(momentous, helpers);

  generators = (function() {
    var nRandomAround, nRandomBetween, outs, randomAround, randomBetween, randomWithin, randomizeAbsDiff, randomizeSignedDiff, _momentCheck;
    randomizeAbsDiff = function(maxDiff) {
      var diff, randomizedDiff;
      diff = Math.abs(maxDiff);
      randomizedDiff = Math.round(Math.random() * diff);
      return randomizedDiff;
    };
    randomizeSignedDiff = function(maxDiff) {
      var diff, sign;
      diff = randomizeAbsDiff(maxDiff);
      sign = Math.random();
      if (sign < 0.5) {
        diff *= -1;
      }
      return diff;
    };
    _momentCheck = function(aMoment) {
      if (!moment.isMoment(aMoment)) {
        return moment(aMoment);
      }
      return aMoment;
    };
    randomBetween = function(startMoment, endMoment, resolution) {
      var diff;
      startMoment = _momentCheck(startMoment);
      endMoment = _momentCheck(endMoment);
      if (resolution == null) {
        resolution = "ms";
      }
      diff = randomizeAbsDiff(startMoment.diff(endMoment, resolution));
      if (startMoment.isBefore(endMoment)) {
        return startMoment.clone().add(resolution, diff);
      } else {
        return endMoment.clone().add(resolution, diff);
      }
    };
    nRandomBetween = function(count, startMoment, endMoment, resolution) {
      var _outs;
      _outs = [];
      while (count--) {
        _outs.push(randomBetween(startMoment, endMoment, resolution));
      }
      return _outs;
    };
    randomAround = function(middleMoment, offset) {
      var diff, maxDiff;
      middleMoment = _momentCheck(middleMoment);
      maxDiff = middleMoment.diff(middleMoment.clone().add(offset));
      diff = randomizeSignedDiff(maxDiff);
      return middleMoment.clone().add(diff);
    };
    nRandomAround = function(count, middleMoment, offset) {
      var _outs;
      _outs = [];
      while (count--) {
        _outs.push(randomAround(middleMoment, offset));
      }
      return _outs;
    };
    randomWithin = reverse2(randomAround);
    return outs = {
      randomAround: randomAround,
      randAround: randomAround,
      nRandomAround: nRandomAround,
      nRandAround: nRandomAround,
      randomWithin: randomWithin,
      randWithin: randomWithin,
      randomBetween: randomBetween,
      randBetween: randomBetween,
      nRandomBetween: nRandomBetween,
      nRandBetween: nRandomBetween
    };
  })();

  _.extend(momentous, generators);

  misc = require(inLib("misc.js"));

  oneRand = (function() {
    var fiveYears, now, _diff;
    now = moment();
    fiveYears = momentous.add.years(now, 5);
    _diff = misc.absDiff(now, fiveYears);
    return function() {
      var diffAmt, sign, _diffAmt, _rnd;
      _rnd = Math.random();
      diffAmt = _diff * _rnd;
      sign = Math.random();
      if (sign < 0.5) {
        _diffAmt = _diffAmt * -1;
      }
      return momentous.add(now, diffAmt);
    };
  })();

  randDates = function(dateCount) {
    var _outs;
    if (dateCount == null) {
      dateCount = 12;
    }
    _outs = [];
    while (dateCount--) {
      _outs.push(oneRand());
    }
    return _outs;
  };

  toStrings = function() {
    var dateList, _strung;
    dateList = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    if (_.isArray(dateList[0])) {
      dateList = dateList[0];
    }
    _strung = _.map(dateList, function(oneDate) {
      return oneDate.format();
    });
    return _strung;
  };

  printem = function(dateList) {
    return console.log(toStrings(eList));
  };

  module.exports = momentous;

}).call(this);
